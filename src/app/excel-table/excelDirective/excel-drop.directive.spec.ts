import { ExcelDropDirective } from './excel-drop.directive';

describe('ExcelDropDirective', () => {
  it('should create an instance', () => {
    const directive = new ExcelDropDirective();
    expect(directive).toBeTruthy();
  });
});
